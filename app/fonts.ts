// app/fonts.ts
import { Inter, Quicksand, Tourney, MuseoModerno } from 'next/font/google'
 
export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
 
export const quicksand = Quicksand({
  subsets: ['latin'],
  display: 'swap',
})

export const tourney = Tourney({
  subsets: ['latin'],
  display: 'swap',
})

export const museoModerno = MuseoModerno({
  subsets: ['latin'],
  display: 'swap',
})